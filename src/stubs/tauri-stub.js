// Stub for web builds — these imports are never actually called on web,
// but Vite needs them to resolve during bundling.
export default {};
export const invoke = () => {};
export const writeText = () => {};
export const load = () => {};
export const getCurrentWindow = () => ({});
